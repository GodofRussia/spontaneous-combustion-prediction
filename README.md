# Система прогнозирования самовозгорания угольных штабелей

Веб-приложение для прогнозирования возгорания угольных штабелей с использованием машинного обучения.
Точность модели: **86.25% (±2 дня)**.

## 🚀 Запуск приложения

### Требования

- Python 3.9+
- Node.js 16+

### Шаг 1: Установка зависимостей

#### Backend

```bash
cd backend
pip install -r requirements.txt
```

#### Frontend

```bash
cd frontend
npm install
```

### Шаг 2: Запуск

#### Запуск Backend (терминал 1)

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Backend доступен по адресу: **<http://localhost:8000>**

#### Запуск Frontend (терминал 2)

```bash
cd frontend
npm run dev
```

Frontend доступен по адресу: **<http://localhost:5173>**

### Шаг 3: Использование

1. Откройте **<http://localhost:5173>** в браузере
2. Перейдите на страницу **Dashboard**
3. Загрузите файлы данных:
   - `supplies.csv` - данные о поставках угля (обязательно)
   - `temperature.csv` - данные о температуре штабелей (обязательно)
   - `weather_data_*.csv` - данные о погоде (опционально)
   - `fires.csv` - реальные данные о возгораниях (опционально, для сравнения)
4. Нажмите **"Сгенерировать прогнозы"**
5. Просмотрите результаты:
   - **Календарь** - визуализация прогнозов по датам
   - **Матрица** - группировка штабелей по датам
   - **Метрики** - оценка точности модели

## 📊 Уровни риска

- 🔴 **Критический** (0-2 дня) - требуется немедленное вмешательство
- 🟠 **Высокий** (3-7 дней) - требуется повышенное внимание
- 🟡 **Средний** (8-14 дней) - требуется мониторинг
- 🟢 **Низкий** (>14 дней) - штатная ситуация

## 🐛 Решение проблем

### Backend не запускается

- Проверьте установку зависимостей: `pip install -r requirements.txt`
- Убедитесь, что порт 8000 свободен
- Проверьте наличие файла модели: `models/fire_prediction_model.pkl`

### Frontend не запускается

- Проверьте установку зависимостей: `npm install`
- Убедитесь, что порт 5173 свободен
- Проверьте, что backend запущен на порту 8000

### Ошибки при загрузке данных

- Проверьте формат CSV файлов (кодировка UTF-8)
- Убедитесь, что названия колонок совпадают с ожидаемыми
- Загрузите минимум `supplies.csv` и `temperature.csv`
