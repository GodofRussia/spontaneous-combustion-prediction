# Coal Fire Prediction - Frontend

React-приложение для прогнозирования возгораний угля на складах.

## Технологии

- **React 18** - UI библиотека
- **React Router** - маршрутизация
- **Vite** - сборщик и dev-сервер
- **Axios** - HTTP клиент
- **FullCalendar** - календарь возгораний
- **Recharts** - графики метрик
- **React Dropzone** - загрузка файлов
- **date-fns** - работа с датами

## Структура проекта

```
frontend/
├── public/
│   └── index.html
├── src/
│   ├── components/          # React компоненты
│   │   ├── Header.jsx       # Шапка приложения
│   │   ├── Sidebar.jsx      # Боковая панель
│   │   ├── FileUpload.jsx   # Загрузка CSV файлов
│   │   ├── PredictionCard.jsx  # Карточка прогноза
│   │   ├── RiskBadge.jsx    # Бейдж уровня риска
│   │   └── MetricsChart.jsx # График метрик
│   ├── pages/               # Страницы приложения
│   │   ├── Dashboard.jsx    # Главная - загрузка данных
│   │   ├── PredictionsCalendar.jsx  # Календарь прогнозов
│   │   └── ModelMetrics.jsx # Метрики модели
│   ├── services/            # Сервисы
│   │   ├── api.js          # API клиент
│   │   └── dataProcessor.js # Обработка данных
│   ├── styles/
│   │   └── App.css         # Стили приложения
│   ├── App.jsx             # Главный компонент
│   └── index.jsx           # Точка входа
├── .env.example            # Пример конфигурации
├── package.json
├── vite.config.js
└── README.md
```

## Установка и запуск

### Предварительные требования

- Node.js 18+ и npm

### Установка зависимостей

```bash
cd frontend
npm install
```

### Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` при необходимости:

```env
VITE_API_URL=http://localhost:8000
VITE_API_TIMEOUT=30000
VITE_ENABLE_METRICS=true
VITE_ENABLE_EXPORT=true
VITE_MAX_FILE_SIZE=52428800
VITE_SUPPORTED_FORMATS=.csv
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

### Предпросмотр продакшен сборки

```bash
npm run preview
```

## Использование

### 1. Загрузка данных

На главной странице загрузите CSV файлы:

- **supplies.csv** (обязательно) - данные о поставках угля
- **temperature.csv** (обязательно) - данные о температуре штабелей
- **weather_data_*.csv** (обязательно) - данные о погоде
- **fires.csv** (опционально) - данные о фактических возгораниях для оценки метрик

Файлы можно перетащить в зону загрузки или выбрать через диалог.

### 2. Выполнение прогноза

После загрузки обязательных файлов нажмите кнопку **"🔮 Выполнить прогноз"**.

Система отправит данные на backend для обработки и прогнозирования.

### 3. Просмотр календаря

На странице **"Календарь"** отображаются прогнозируемые даты возгораний:

- 🔴 **Критический** (0-2 дня) - требуется немедленное действие
- 🟠 **Высокий** (3-7 дней) - высокий приоритет мониторинга
- 🟡 **Средний** (8-14 дней) - регулярный мониторинг
- 🟢 **Низкий** (>14 дней) - нормальный статус

Нажмите на событие в календаре для просмотра детальной информации о штабеле.

### 4. Просмотр метрик

На странице **"Метрики"** отображаются:

- **MAE** (Mean Absolute Error) - средняя абсолютная ошибка в днях
- **Точность ±2 дня** - процент прогнозов с точностью ±2 дня (целевая метрика ≥70%)
- **Графики точности** - визуализация точности для разных допусков
- **Детальная статистика** - таблица со всеми метриками

## Интеграция с Backend

Приложение взаимодействует с FastAPI backend через REST API:

### Endpoints

- `POST /api/v1/upload/supplies` - загрузка supplies.csv
- `POST /api/v1/upload/temperature` - загрузка temperature.csv
- `POST /api/v1/upload/weather` - загрузка weather_data_*.csv
- `POST /api/v1/upload/fires` - загрузка fires.csv
- `POST /api/v1/predict` - запуск прогнозирования
- `GET /api/v1/predict/{prediction_id}` - получение результатов
- `POST /api/v1/metrics/evaluate` - оценка метрик модели
- `GET /api/v1/metrics/summary` - сводка метрик

### Формат данных

API возвращает данные в JSON формате. Примеры см. в `ARCHITECTURE.md`.

## Особенности реализации

### Обработка ошибок

- Валидация файлов на клиенте (тип, размер)
- Отображение ошибок загрузки и API
- Graceful degradation при недоступности backend

### Адаптивность

- Responsive дизайн для desktop и tablet
- Мобильная версия с упрощенным интерфейсом

### Производительность

- Lazy loading компонентов
- Мемоизация вычислений
- Оптимизация рендеринга календаря

### UX

- Drag-and-drop загрузка файлов
- Индикаторы загрузки
- Уведомления об успехе/ошибке
- Интерактивный календарь
- Детальные карточки прогнозов

## Разработка

### Структура компонентов

Компоненты организованы по принципу:
- **Презентационные** - отображение UI (PredictionCard, RiskBadge)
- **Контейнеры** - логика и состояние (Dashboard, PredictionsCalendar)
- **Утилиты** - переиспользуемые функции (dataProcessor)

### Стилизация

Используется CSS с БЭМ-подобной методологией:
- Модульные стили для компонентов
- CSS переменные для цветов риска
- Responsive breakpoints

### API клиент

Axios с interceptors для:
- Автоматической обработки ошибок
- Добавления заголовков
- Трансформации ответов

## Troubleshooting

### Ошибка подключения к API

Проверьте:
1. Backend запущен на http://localhost:8000
2. CORS настроен правильно в backend
3. `VITE_API_URL` в `.env` указывает на правильный адрес

### Файлы не загружаются

Проверьте:
1. Формат файла - должен быть `.csv`
2. Размер файла - не более 50 МБ
3. Структура CSV соответствует требованиям

### Календарь не отображается

Проверьте:
1. Данные прогнозов загружены
2. Формат дат корректный (ISO 8601)
3. Нет ошибок в консоли браузера

## Лицензия

Проект разработан для хакатона по прогнозированию возгораний угля.

## Контакты

Для вопросов и предложений создайте issue в репозитории проекта.